# LearnOS 开发规则

## 产品原则

- LearnOS 是单用户、用户自托管的长期学习系统，不按 SaaS 多租户设计。
- SQLite 是结构化事实来源；Markdown 是可再生成的导出格式。
- 程序负责状态、记忆、流程和审计；LLM 负责推理、评价、解释和生成。
- 学科知识地图决定“学什么”；个人档案只影响顺序、案例、深度和实践。
- 每次迭代必须形成可运行、可验证的垂直功能，不一次铺开全部模块。

## 工程规则

- 后端使用 Go，保持 handler → service → repository 分层。
- 前端使用 Vue 3、TypeScript、Vue Router 和 Element Plus。
- API 使用 `/api/v1` 前缀；错误响应不得泄漏内部堆栈。
- 所有数据库变更必须有可重复执行的迁移策略。
- AI 输出必须经过结构校验后才能更新课程状态。
- 不将 API Key、密码明文或个人学习数据提交到仓库。
- 修改功能时同步更新 `docs/ROADMAP.md` 和相关设计文档。

## 每次 Codex 任务的执行方式

1. 先阅读相关文档和现有实现。
2. 给出本次修改范围，不擅自扩大需求。
3. 完成代码、测试和必要文档。
4. 执行格式化、测试与构建。
5. 汇报变更、验证结果和仍存在的风险。
